# imdb-sentiment-bilstm-glove
his project performs binary sentiment classification on IMDb movie reviews using a Bidirectional LSTM (BiLSTM) neural network with pre-trained GloVe word embeddings. The model achieves high accuracy by combining deep learning with rich semantic representations of words.
